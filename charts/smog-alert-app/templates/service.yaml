apiVersion: v1
kind: Service
metadata:
  name: {{ include "smog-alert-app.fullname" . }}
  labels:
    {{- include "smog-alert-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smog-alert-app.selectorLabels" . | nindent 4 }}
