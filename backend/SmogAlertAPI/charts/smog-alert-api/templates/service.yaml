apiVersion: v1
kind: Service
metadata:
  name: {{ template "smog-alert-api.fullname" . }}
  labels:
    app: {{ template "smog-alert-api.name" . }}
    chart: {{ template "smog-alert-api.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "smog-alert-api.name" . }}
    release: {{ .Release.Name }}
